// This file is unused. The dashboard now queues analysis requests to
// data/analysis-queue/ instead of calling the Anthropic API directly.
// Kept as an empty stub because Windows-mounted filesystem permissions
// prevent deletion from this build environment.
export {};
