# Postgres connection string used by the dashboard and migration script.
# Copy this file to `.env.local` and adjust as needed.
DATABASE_URL=postgresql://postgres:1@localhost:5432/tradesss?pool_timeout=0
